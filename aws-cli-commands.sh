# Command to check the AWS CLI version
aws --version
# Command to configure AWS CLI with access key, secret key, and default region
aws configure
# Command to list the current AWS Identity
aws sts get-caller-identity
# Command to list all S3 buckets in the account
aws s3 ls